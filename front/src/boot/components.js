import Vue from 'vue';

import globalcomponents from "../components/install/globalcomponents";
Vue.use(globalcomponents);

export default async ({
  /* app, router, Vue, ... */
}) => {
  // something to do
}
